<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/distance_8dp">

    <androidx.cardview.widget.CardView
        android:layout_width="@dimen/filter_preview_size"
        android:layout_height="@dimen/filter_preview_size"
        app:cardCornerRadius="@dimen/distance_corner_radius_35dp"
        app:cardElevation="@dimen/distance_0dp"
        app:cardBackgroundColor="@color/transparent">

        <ImageView
            android:id="@+id/filter_preview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@string/filter_normal" />

        <View
            android:id="@+id/filter_border"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/circle_border"
            android:visibility="gone" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/filter_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/distance_4dp"
        android:textColor="@color/white"
        android:textSize="@dimen/size_12sp" />

</LinearLayout>
